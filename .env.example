# URL do Nest (BFF) — no browser usa localhost; no Docker SSR usa o serviço `nest`.
NEXT_PUBLIC_API_URL=http://127.0.0.1:4000

# Apenas servidor (Next em Docker): hostname interno do BFF
# API_INTERNAL_URL=http://nest:4000
