# URL do Rails — no browser usa localhost; no Docker (SSR) usa o serviço `app`.
NEXT_PUBLIC_RAILS_URL=http://127.0.0.1:3000

# Apenas servidor (Next em Docker): hostname interno do Rails
RAILS_INTERNAL_URL=http://app:3000
