# Standalone entrypoint for Next.js + Rails + shared infra (Postgres + Redis).
#
# Usage (from this directory):
#   docker compose -f compose.standalone.yml up --build
#
include:
  - path: ../poc-finance-base/compose.infra.yml
  - path: ../poc-finance-rails/compose.yml
  - path: ./compose.yml

